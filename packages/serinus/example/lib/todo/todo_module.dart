import 'package:serinus/serinus.dart';

/// A module for managing todo items.
class TodoModule extends Module {
  /// Creates a [TodoModule].
  TodoModule() : super(imports: [], controllers: [], providers: []);
}
